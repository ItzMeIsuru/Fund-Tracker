{
  "name": "finance-tracker",
  "version": "1.0.0",
  "description": "Student Finance Tracker with Neon DB sync",
  "main": "app.js",
  "dependencies": {
    "@neondatabase/serverless": "^0.6.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
